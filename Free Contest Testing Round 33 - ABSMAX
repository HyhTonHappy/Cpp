#include <bits/stdc++.h>
using namespace std;

#define ll long long

ll n;
ll a[200005];



int main() {
cin >> n;
for(int i = 1;i <=n;i++){
    cin >> a[i];
}
sort(a + 1, a + n + 1);
ll maxx = (ll) max(abs(a[1] + a[2]), abs(a[n] + a[n - 1]));
cout << maxx;
    return 0;
}
